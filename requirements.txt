Django>=5.1,<5.2
psycopg[binary]>=3.1
python-dotenv>=1.0
whitenoise>=6.6
gunicorn>=21.2
# Certificaciones y planillas en PDF. Necesita libpango en el sistema
# (ya incluida en el Dockerfile).
weasyprint>=62
